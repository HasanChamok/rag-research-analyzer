[project]
name = "ragcore"
version = "0.1.0"
description = "A reusable RAG engine for research paper analysis"
requires-python = ">=3.11"
depenedencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"